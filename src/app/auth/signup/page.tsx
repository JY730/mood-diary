import React from 'react';
import AuthSignup from '@/components/auth-signup';

/**
 * 회원가입 페이지
 * 레이아웃을 사용하지 않아 max-width 제한이 없습니다.
 */
export default function SignupPage() {
  return <AuthSignup />;
}

